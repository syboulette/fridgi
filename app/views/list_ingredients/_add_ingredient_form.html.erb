<%= simple_form_for [@list, @list_ingredient] do |f| %>
  <%= f.input :ingredient_id, label: false, input_html:{ id: 'ingredient-field' } %>
  <%= f.input :quantity, label: false %>
  <%= f.input :unit, label: false %>
  <%= image_submit_tag ("plus.svg"), class: "submit_button", data: { method: :post }%>
<% end %>
