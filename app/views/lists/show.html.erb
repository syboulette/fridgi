<div class="content-page">
  <div class="list">
    <div class="inside-list">
      <div class="header-list">
        <h1 class="page_title"><%=@list.name%></h1>
        <p class="page_subtitle">There is nothing more important than a well made list. </p>
        <p class="page_subtitle">So you don't buy useless things or forget the important ones!</p>
      </div>
      <% if @list_ingredients.any? %>
        <div class="line">
          <h6 class="header_list ingredient">Ingredients</h6>
          <h6 class="header_list quantity">Quantity</h6>
          <h6 class="header_list unit">Units</h6>
          <h6 class="header_list edit">Edit</h6>
        </div>
        <%= form_tag list_copy_to_fridge_lists_path(@list) do %>
          <% @list_ingredients.each do |list_ingredient| %>
              <div class="line">
                <h6 class="content_list ingredient">
                  <%= check_box_tag "list_ingredient_ids[]", list_ingredient.id %>
                  <%= list_ingredient.ingredient.name %></h6>
                <h6 class="content_list quantity"><%= list_ingredient.quantity %></h6>
                <h6 class="content_list unit"><%= list_ingredient.unit %></h6>
                <div class="content_list icon">
                  <%= link_to image_tag("edit.svg"), edit_list_list_ingredient_path(@list, list_ingredient), class: "edit-link", data: { turbo_frame: "edit-form", action: "click->list-ingredients-edit-form#openEditForm" } %>
                  <%= link_to image_tag("bin.svg"), list_ingredient_path(list_ingredient), data: { confirm: 'Are you sure?', turbo_frame: '_top', method: :delete } %>
                </div>
              </div>
              <turbo-frame id="edit-form"></turbo-frame>
          <% end %>
          <%= submit_tag "Copy to Fridge" %>
        <% end %>
      <% else %>
        <h6>Oups! Your list is empty...</h6>
      <% end %>
    </div>
  </div>
  <div class="add_ingredient">
    <%= render "list_ingredients/add_ingredient_form", edit: false %>
  </div>
</div>


