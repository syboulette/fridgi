<%# it is only to show the reults, it is NOT in form! %>

<!-- Ariane's stuff
<h1>Cookbook!</h1>
<p><%= link_to "my recipes", recipes_user_recipes_path, class: "btn btn-info btn-sm "%></p>
<p><%= link_to "Favourites", favourite_recipes_path, class: "btn btn-info btn-sm "%></p>
<div class="container">
<div class="cards">
  <% @recipes.each do |recipe| %>
    <div class="card">
      <h6 class="card-text recipe-title"><em><%= link_to recipe.title, recipe_path(recipe)%></em></h6>
      <div class="inner-flex">
        <%= button_to "Delete", recipe, method: :delete, class:"btn btn-danger" %>
      </div>
      <p class="card-text recipe-extract">You have <%= recipe.recipe_ingredients.count %> ingredients in this recipe</p>
      <p class="card-text recipe-extract">Created on <%= recipe.created_at.strftime("%d %b %Y") %></p>
      <p><%= link_to "open my recipe", recipe_path(recipe), class: "btn btn-info btn-sm "%></p>
    </div>
  <% end %>
</div>
</div>
-->
<div class="content" id="content-recipe">
  <p><%= link_to "create a new recipe!", new_recipe_path, class: "btn btn-info btn-sm "%></p>
  <div class="box-grid">
    <% @recipes.each do |recipe| %>
      <% if recipe.image.attached? %>
        <%= render "card_header_image", recipe: recipe%>
      <% else %>
        <%= render "card_header_text", recipe: recipe%>
      <% end %>
    <% end %>
  </div>
</div>
